{
  "compilerOptions": {
    "target": "es6",              /* ECMA Script 버전을 'ES3'(기본값), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2019', 'ES2021' 또는 'ESNEXT'로 지정합니다.  */
    "module": "commonjs",         /* 모듈을 위한 코드 생성 설정: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', 'es2020', or 'ESNext'. */
    "strict": true,               /* 모든 엄격한 타입-체킹 옵션 활성화 여부 */

    "moduleResolution": "node",   /* 모듈 해석 방법 설정: 'node' (Node.js) 혹은 'classic' (TypeScript pre-1.6). */
    "resolveJsonModule": true,    /*  TypeScript는 기본적으로 JSON 파일 확인을 지원하지 않음, 이 옵션을 true로 변경하면 지원 가능함. */
    "esModuleInterop": true,      /* 모든 imports에 대한 namespace 생성을 통해 CommonJS와 ES Modules 간의 상호 운용성이 생기게할 지 여부,
                                    'allowSyntheticDefaultImports'를 암시적으로 승인합니다. */

    "outDir": "./dist/",        /* 해당 디렉토리로 결과 구조를 보냅니다. */
    "baseUrl": "./",
    "paths": { "@common/*": ["../common/*"] }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}

