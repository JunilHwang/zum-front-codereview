{
  "name": "backend",
  "version": "0.1.0",
  "description": "[ZUM] 프론트엔드 개발자 (신입/경력) 과제 - Backend",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch \"src/**/*.ts\" --exec \"ts-node\" ./src/index.ts",
    "build": "tsc -p tsconfig.json",
    "build:start": "tsc -p tsconfig.json && node ./dist/backend/src/index.js"
  },
  "author": "Hoyoung Son",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.10",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}
