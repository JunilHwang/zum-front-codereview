{
  "compilerOptions": {
    "baseUrl": "./",
    "target": "es6", // ECMA Script 버전을 'ES3'(기본값), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2019', 'ES2021' 또는 'ESNEXT'로 지정
    "module": "esnext", // 모듈을 위한 코드 생성 설정: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', 'es2020', or 'ESNext'.
    "lib": ["dom", "DOM.Iterable", "ESNext"], // 컴파일에 포함될 라이브러리 파일 목록
    "allowJs": true, // 자바스크립트 파일 컴파일 허용 여부
    "sourceMap": true, // .map 파일 생성 여부
    "outDir": "./dist/",
    "isolatedModules": true, // 각 파일을 분리된 모듈로 트랜스파일 ('ts.transpileModule'과 비슷)
    "strict": true,
    "noFallthroughCasesInSwitch": true, // switch문에서 fallthrough 케이스에 대한 에러보고 여부
    "moduleResolution": "node", // 모듈 해석 방법 설정: 'node' (Node.js) 혹은 'classic' (TypeScript pre-1.6).
    "esModuleInterop": true, // 모든 imports에 대한 namespace 생성을 통해 CommonJS와 ES Modules 간의 상호 운용성이 생기게할 지 여부, 'allowSyntheticDefaultImports'를 암시적으로 승인.)
    "skipLibCheck": true, // 정의 파일의 타입 확인을 건너 뛸 지 여부
    "forceConsistentCasingInFileNames": true, // 같은 파일에 대한 일관되지 않은 참조를 허용하지 않을 지 여부
    "paths": { "@src/*": ["src/*"], "@public/*": ["public/*"] },
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules/"]
}
